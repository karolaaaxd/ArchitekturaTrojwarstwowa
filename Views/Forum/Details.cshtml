@model ASP.NET_CoreWebApplicationPage.Models.Forum

@{
    ViewData["Title"] = "Więcej";
}

<h1>Więcej</h1>


<div>

    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Post)
        </dt>
        <dd class="col-sm-10">
          
                @Html.DisplayFor(model => model.Post)
            
            </dd>
    </dl>
</div>
<div>
 
        <a asp-action="Index" class="btn btn-light">Wróc do listy</a>
         <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-light">Edit</a>  
         <a asp-action="Delete" asp-route-id="@Model.Id" class="btn btn-danger">Usuń</a>

  
</div>

